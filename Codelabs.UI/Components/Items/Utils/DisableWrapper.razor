<div class="@WrapperClass">
    @ChildContent
</div>
@code {
    [Parameter] public bool IsDisable { get; set; } = false;
    [Parameter] public RenderFragment? ChildContent { get; set; }

    private string WrapperClass => IsDisable ? "disabler" : string.Empty;
}